# PII redaction policy — all categories, placeholder strategy
name: "pii-demo"
version: "1.0"

rules:
  allowed_tools: null  # Allow all tools
  denied_tools: []

  pii_redaction:
    enabled: true
    categories:
      - email
      - phone
      - ssn
      - credit_card
      - ip_address
    strategy: "placeholder"

  resource_limits: {}

on_violation: "block"
