# Allows everything, blocks nothing
name: "allow-all"
version: "1.0"

rules:
  allowed_tools: null  # null = all tools allowed
  denied_tools: []
  pii_redaction:
    enabled: false
  resource_limits: {}

on_violation: "block"
